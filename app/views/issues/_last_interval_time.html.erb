<% if @project.module_enabled?(:tiempo_entre_estados) and User.current.allowed_to?(:tee_view_time, @project) %>
<tr>
	<th class="category">Tiempo en el estado actual:</th>
	<td class="category"><%= (@last_interval_time * 10).floor / 10.0 %> <%= l(:"issue.hour")+"(s)" %></td>
</tr>
<% end %>